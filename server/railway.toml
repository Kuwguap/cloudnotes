[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
restartPolicy = "always"
maxRetries = 10

[deploy.env]
NODE_ENV = "production"
RAILWAY_ENVIRONMENT = "production"